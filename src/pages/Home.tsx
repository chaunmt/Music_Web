import { TrackController } from "../routes"
import "./Home.css"

export const Home = () => { // Export Home as the declared function
  return (
    <div>
      < TrackController />
    </div>
  )
}